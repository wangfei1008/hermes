# hermes
1、做一个数采软件架构，实现数据采集、分析、转发、控制。
2、软件分为主程序(可执行文件)和组件（dll）。主程序主要作用是组装组件，让组件按照设计的流程运行。组件可以根据需要随时新添和删除。
3、主程序负责按照数据库配置启动组件。
4、主程序启动一个总缓存，用于各个设备采集结果数据的存储，和转发组件获取数据。
5、主程序建立一个消息通道，用于设备之间，或者外部发生消息控制设备。
6、可以同时采集多个设备，一个设备包括多个组件流，每个组件流由多个组件组成，如采集、分析、计算，执行。
